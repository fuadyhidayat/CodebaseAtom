@inherits DialogBase

<MudDialog>
    <DialogContent>
        <MudStack Spacing="4" Class="mb-3">
            <ErrorViewer Exception="ExceptionBase" />
            @if (_input is not null)
            {
                <EditForm Model="_input" OnValidSubmit="OnValidSubmitAsync">
                    <DataAnnotationsValidator />
                    <GridDefault>
                        <MudItem xs="12" sm="6">
                            <TextFieldDefault Label="@DomainDisplayTextFor.Title"
                                              Placeholder="@($"Enter {DomainDisplayTextFor.WorkItem} {DomainDisplayTextFor.Title}")"
                                              @bind-Value="_input.Title"
                                              For="@(() => _input.Title)" />
                        </MudItem>
                        <MudItem xs="12" sm="6">
                            <MudDatePicker Label="@DomainDisplayTextFor.Deadline"
                                           @bind-Date="DeadlineDateTime"
                                           DateFormat="dd/MM/yyyy"
                                           Required="true"
                                           Variant="Variant.Outlined"
                                           RequiredError="@($"{DomainDisplayTextFor.Deadline} is required")"
                                           MinDate="@DateTime.Today" />
                        </MudItem>
                        <MudItem xs="12">
                            <TextFieldMultiline Label="@DomainDisplayTextFor.Description"
                                                Placeholder="@($"Enter {DomainDisplayTextFor.WorkItem} {DomainDisplayTextFor.Description}")"
                                                Lines="3"
                                                MaxLines="5"
                                                @bind-Value="_input.Description"
                                                For="@(() => _input.Description)" />
                        </MudItem>
                    </GridDefault>
                    <BottomButtonPanel>
                        <ButtonSave ButtonType="ButtonType.Submit" />
                    </BottomButtonPanel>
                </EditForm>
            }
        </MudStack>
    </DialogContent>
</MudDialog>
